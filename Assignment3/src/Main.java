public class Main {
    /**
     * The main method of the program.
     * It creates an instance of the GeneticAlgorithm class and starts the genetic
     * algorithm process.
     *
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        GeneticAlgorithm ga = new GeneticAlgorithm();
        ga.run(); // Start the genetic algorithm process
    }
}
